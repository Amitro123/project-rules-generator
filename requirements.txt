# Core (always installed)
click>=8.0.0,<9.0.0
pyyaml>=6.0.0,<7.0.0
pydantic>=2.0.0,<3.0.0
tqdm>=4.66.0,<5.0.0
python-dotenv>=1.0.0,<2.0.0
gitpython>=3.1.0,<4.0.0
rich>=13.0.0,<14.0.0
pathspec>=0.11.0
jinja2>=3.1.0,<4.0.0
watchdog>=3.0.0,<5.0.0
packaging>=23.0

# LLM providers are optional extras — do NOT install from this file.
# Use: pip install "project-rules-generator[llm]"  (all providers)
#   or: pip install "project-rules-generator[anthropic]"  (single provider)
# See pyproject.toml [project.optional-dependencies] for the full list.
