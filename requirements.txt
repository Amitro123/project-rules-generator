# pyproject.toml is the single source of truth for dependency constraints.
# This file is a contributor convenience: an editable install of PRG plus
# all four LLM providers.
#
#   pip install -r requirements.txt        # editable install + all providers
#
# For a single provider instead of all four, install the matching extra:
#   pip install -e ".[anthropic]"          # or .[gemini] / .[groq] / .[openai]
#
# Core (non-LLM) dependencies are declared in pyproject.toml [project].
-e .[llm]
