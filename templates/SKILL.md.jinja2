---
name: {{ name }}
description: |
  {{ desc_with_triggers }}
license: MIT
allowed-tools: "Bash Read Write Edit Glob Grep"
metadata:
  author: PRG
  version: 1.0.0
  category: {{ category }}
  tags: [{{ tags | join(', ') }}]
---

# Skill: {{ title }}

## Purpose

{{ purpose }}

{{ purpose_extended }}

## Auto-Trigger

The agent should activate this skill when the user requests:

{% for trigger in auto_triggers -%}
- **"{{ trigger }}"**
{% endfor %}

**Project Context Signals:**
{% if project_signals %}
{% for signal in project_signals -%}
- `{{ signal }}` {% if signal == 'has_docker' %}Γזע Docker environment detected{% elif signal == 'has_tests' %}Γזע Test suite available{% elif signal == 'has_api' %}Γזע API endpoints present{% endif %}
{% endfor %}
{% else %}
- No specific signals required
{% endif %}

## Process

### 1. {{ step1_title | default("Analyze Current State") }}

{{ step1_content | default("- Review project structure in `" + project_name + "/`\n- Check configuration files\n- Identify existing patterns") }}

### 2. {{ step2_title | default("Execute Core Actions") }}

**Required Tools:** {% for tool in tools %}`{{ tool }}`{% if not loop.last %}, {% endif %}{% endfor %}

{{ step2_content | default("```bash\n# Navigate to project\ncd " + project_name + "\n\n# Execute skill-specific commands\n# (customize based on detected tech stack)\n```") }}

### 3. {{ step3_title | default("Validate & Verify") }}

{{ step3_content | default("- Run tests if available\n- Check for errors\n- Verify expected outputs") }}

{% if additional_steps %}
{% for step in additional_steps %}
### {{ loop.index + 3 }}. {{ step.title }}

{{ step.content }}
{% endfor %}
{% endif %}

## Output

This skill generates:

{% if outputs %}
{% for output in outputs -%}
- {{ output }}
{% endfor %}
{% else %}
- Modified/created files in `{{ project_name }}/`
- Status report with changes
- Recommendations for next steps
{% endif %}

## Anti-Patterns

{% if anti_patterns %}
{% for pattern in anti_patterns %}
{{ pattern }}
{% endfor %}
{% else %}
❌ **Don't** use generic commands without project context
✅ **Do** reference actual files from `{{ project_name }}/`

❌ **Don't** skip validation steps
✅ **Do** always verify changes with tests

❌ **Don't** make assumptions about project structure
✅ **Do** check for files/directories before operating on them
{% endif %}

## Tech Stack Notes

{% if tech_stack %}
**Detected Technologies:**
{% for tech in tech_stack -%}
- `{{ tech }}`
{% endfor %}
{% endif %}

**Compatible Tools:** {% for tool in tools %}`{{ tool }}`{% if not loop.last %}, {% endif %}{% endfor %}

{% if tech_notes %}

{{ tech_notes }}
{% endif %}

## Examples

{% if examples %}
{% for example in examples %}
### Example {{ loop.index }}: {{ example.title }}

{{ example.content }}
{% endfor %}
{% else %}
```bash
# Basic usage workflow
cd {{ project_name }}

# 1. Check status
git status

# 2. Run skill-specific command
# (customize based on actual project needs)

# 3. Verify results
{% if 'pytest' in tools %}pytest{% elif 'npm' in tools %}npm test{% else %}# Run appropriate validation{% endif %}
```
{% endif %}

## Project Context

```
Project: {{ project_name }}
Path: {{ project_path }}
Signals: {{ project_signals | join(', ') }}
Tech Stack: {{ tech_stack | join(', ') }}
```

{% if readme_context %}
### README Context

{{ readme_context[:500] }}...
{% endif %}

---

*Generated by Cowork-Powered PRG Skill Creator v2.0*
*Triggers: {{ auto_triggers | length }} | Tools: {{ tools | length }}*
