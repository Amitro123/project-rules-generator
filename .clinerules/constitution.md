**File:** constitution.md

**Project Rules Generator — Constitution**

> This document outlines the coding principles, standards, and best practices for the project. It is auto-generated and reflects the actual tools, patterns, and conventions found in the codebase.

---

## Code Quality Principles

### Linting & Formatting

- **Run linter and formatter before committing**: Execute `ruff check .` and `mypy .` to ensure code quality.
- **Automated checks**: Set up CI/CD pipelines to run these checks automatically on every push.

### Type Safety

- **Enforce type hints**: Use type hints on all public APIs to ensure type safety.
- **Pydantic models**: Use Pydantic models for data validation and serialization to catch errors early.
- **Example**:
```python
from pydantic import BaseModel

class User(BaseModel):
    id: int
    name: str
    email: str
```

### Input Validation

- **Validate inputs**: Use Pydantic models to validate inputs instead of raw dictionaries.
- **Example**:
```python
from pydantic import BaseModel

class UserInput(BaseModel):
    id: int
    name: str
    email: str

user_input = UserInput(id=1, name="John Doe", email="john@example.com")
```

## Testing Standards

- **Testing framework**: Use pytest (31 test files, 218 test cases) for testing.
- **Run tests**: Execute `pytest` before merging any changes.
- **Shared fixtures**: Use shared fixtures from `conftest.py` to avoid duplicating test setup.
- **Test data**: Store test data in `tests/fixtures/`.
- **Test types**: Write unit and integration tests for every new feature or bug fix.
- **Example**:
```python
import pytest

@pytest.fixture
def user():
    return {"id": 1, "name": "John Doe", "email": "john@example.com"}

def test_user(user):
    assert user["id"] == 1
    assert user["name"] == "John Doe"
    assert user["email"] == "john@example.com"
```

## Architecture Decisions

- **Project type**: The project is a Python CLI application.
- **Entry points**: The entry point of the application is `main.py`.
- **Structural patterns**: Use the python-cli and pytest-tests structural patterns.
- **Click decorators**: Use Click decorators for CLI commands to keep command functions thin.
- **Example**:
```python
import click

@click.command()
def main():
    click.echo("Hello, World!")

if __name__ == "__main__":
    main()
```

## Development Guidelines

### Detected Tools

- **Test runner**: `pytest`
- **Linter**: `ruff`
- **Formatter**: `mypy`

### Key Paths

- `main.py`: The entry point of the application.

### Version Control

- **Commit secrets**: Never commit secrets, API keys, or `.env` files.
- **Commit messages**: Write descriptive commit messages following conventional commits.
- **Module imports**: Keep module imports at the top of each file; use absolute imports within the project.

---
_Generated by project-rules-generator (constitution mode)_