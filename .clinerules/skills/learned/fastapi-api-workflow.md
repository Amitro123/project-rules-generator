---
name: fastapi-api-workflow
description: '# Requires GEMINI_API_KEY'
auto_triggers:
  keywords:
  - audit fastapi
  - check fastapi api workflow
  - create fastapi api workflow
  - fastapi api workflow
  - fastendpoint endpoint workflow
  - generate fastapi api workflow
  - review fastapi
  - run fastapi api workflow
  project_signals:
  - has_tests
  - has_api
  - has_ci
  - has_docs
tools:
- curl
- docker
- pydantic
category: project
---

# Skill: Fastapi Api Workflow

## Purpose

# Requires GEMINI_API_KEY

This skill provides step-by-step guidance for fastapi api workflow.

## Auto-Trigger

The agent should activate this skill when:

- audit fastapi
- check fastapi api workflow
- create fastapi api workflow
- fastapi api workflow
- fastendpoint endpoint workflow
- generate fastapi api workflow
- review fastapi
- run fastapi api workflow

**Project Signals:**
- `has_tests`
- `has_api`
- `has_ci`
- `has_docs`

## Process

### 1. Analyze Current State

- Check project structure in `project-rules-generator/`
- Review relevant configuration files
- Identify existing patterns

### 2. Execute Core Steps

**Tools Required:** `curl`, `docker`, `pydantic`

```bash
# Example workflow
cd project-rules-generator
# Run appropriate commands based on skill context
```

### 3. Validate Results

- Verify expected outputs
- Run tests if applicable
- Check for common issues

## Output

This skill generates:

- Modified/created files in project
- Status report of changes
- Recommendations for next steps

## Anti-Patterns

❌ **Don't** use generic commands without project context
✅ **Do** use actual project paths and configurations

❌ **Don't** skip validation steps
✅ **Do** always verify changes work as expected

## Tech Stack Notes

**Detected Technologies:** docker, pydantic, redis, fastapi, pytest, python, react

**Compatible Tools:** curl, docker, pydantic

## Project Context

```
Project: project-rules-generator
Signals: has_tests, has_api, has_ci, has_docs
```

---
*Generated by Cowork-Powered PRG Skill Creator*
